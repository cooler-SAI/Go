syntax = "proto3";

package example;

option go_package = "proto/example;example";

service Haberdasher {
  rpc MakeHat(Size) returns (Hat);
}

message Size {
  int32 inches = 1;
}

message Hat {
  string color = 1;
  int32 inches = 2;
}
